# Phase 4 —— ⚠️ 已降级，不再是独立 phase

> **实测依据**：AndarBahar000001 那次，铁律自查跑了两次、**两次都是 0 条新发现**；
> 同一批代码的独立审查揪出 36 条（资金阻断级 12 条）。
> 原因是自查在拿自己的心智模型核对自己写的代码——**前提错了就一起错**。
> 本次最贵的那条（走势串归属不可判定）不违反任何一条已知铁律，自查永远查不出来。

## 现在怎么做

**把下面的检查表并进 Phase 6**，当**回归性检查**用（确认既有铁律没被新改动破坏），
**不要**再单独占一个 phase、不要再写 `self-review.md`。

- known-pitfalls **C 节逐条**核对，每条给出 file:line 证据（不是「应该没问题」）。
- 本族相对模板的差异逐条列出（哪些是刻意不做，不是漏做）。
- 发现缺口靠**独立视角 + 结构化攻击 + 多轮迭代**，不靠自查。

## 保留的价值

自查唯一稳定产出的东西是**「本族刻意不做什么」的清单**——它在经验文档和后续机台里有复用价值
（例：本族无 `restore.begin/end` 所以不补 `isRestoring`；本桌 config 无 payout cap 字段所以不臆造）。
这部分直接写进 Phase 7 的经验文档，不必单独成文。
